{
  "install_cmd": "corepack enable && pnpm install --frozen-lockfile --config.dangerouslyAllowAllBuilds=true",
  "start_cmd": "node node_modules/next/dist/bin/next start -p ${PORT:-3000}",
  "mode": "port",
  "host_port": 3001,
  "service_port": 3000,
  "engine": "docker"
}
